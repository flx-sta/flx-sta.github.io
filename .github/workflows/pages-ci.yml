on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run deploy.sh script
        run: ./.github/scripts/deploy.sh
        shell: bash
